@model User
@{

}
<div>
   <form asp-action="SetUser" asp-route-id="@Model.Id">
        <h2>Godkend skift til brugerprofil @Model.Name</h2>
        <div class="form-group">
            <input type="submit" value="Godkend" class="btn btn-primary" />
        </div>
   </form>
</div>

