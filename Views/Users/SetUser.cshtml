@model User
@{
    ViewData["Title"] = "Vælg brugerprofil";
}
<div>
   <form asp-action="SetUser" asp-route-id="@Model.Id">
        <p>Godkend skift til brugerprofil @Model.Name</p>
        <div class="form-group">
            <input type="submit" value="Godkend" class="btn btn-primary" />
        </div>
   </form>
</div>

